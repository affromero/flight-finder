node_modules
**/node_modules
.next
**/.next
dist
**/dist
**/*.tsbuildinfo
.git
.github
.claude
.venv
**/__pycache__
.DS_Store
*.log

# Keep secrets out of the build context (M10)
.env
.env.*
!.env.example

# Analytics SQLite database and WAL sidecars must not be baked into the image
# (INFRA-4). The runtime uses a named volume at /app/data instead.
apps/web/data/**
!apps/web/data/car-locations/manifest.json
!apps/web/data/car-locations/catalog.json.gz
!apps/web/data/car-locations/ATTRIBUTION.txt

# Prisma 7 client is regenerated in the builder stage
apps/web/src/generated
