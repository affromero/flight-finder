#!/usr/bin/env bash
# Local dev wrapper — looks like the real `fairtrail` command in recordings
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../../.."
exec node --import tsx --import ./packages/cli/register.mjs packages/cli/src/index.tsx "$@"
